A program was used to encrypt `flag14`'s password. It encrypts input
and writes it to standard output. An encrypted `passwd.txt` file
already exists. Can you decrypt it?

Files for this level can be found in `/home/flag14`.

Run `getflag` as `flag14` to complete this level.
