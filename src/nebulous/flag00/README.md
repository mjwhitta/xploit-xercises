This level requires you to find a Set User ID (SUID) program that will
run as the `flag00` account. You could find this by carefully looking
in directories in your `$PATH` for suspicious looking files or
subdirectories.

Alternatively, look at the `find` man page.

Run `getflag` as `flag00` to complete this level.
