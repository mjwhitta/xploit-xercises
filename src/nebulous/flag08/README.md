World readable files strike again. Check what the `flag08` user was up
to, and use it to log into their account.

Files for this level can be found in `/home/flag08`.

Run `getflag` as `flag08` to complete this level.
