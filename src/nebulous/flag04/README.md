This level requires you to read the `passwd.txt` file, but `flag04`'s
code restricts the files that can be read. Find a way to bypass it.

Files for this level can be found in `/home/flag04`.

Run `getflag` as `flag04` to complete this level.
