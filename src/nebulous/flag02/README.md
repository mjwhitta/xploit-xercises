There is a vulnerability in `flag02`'s program that allows arbitrary
programs to be executed, can you find it?

Files for this level can be found in `/home/flag02`.

Run `getflag` as `flag02` to complete this level.
