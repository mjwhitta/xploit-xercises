There is a vulnerability in `flag01`'s program that allows arbitrary
programs to be executed, can you find it?

Files for this level can be found in `/home/flag01`.

Run `getflag` as `flag01` to complete this level.
