The SUID binary created by `flag10` will upload any file given, as
long as it meets the requirements of the `access()` system call.

Files for this level can be found in `/home/flag10`.

Run `getflag` as `flag10` to complete this level.
