The `flag07` user was writing their very first Go server program that
allowed them to ping hosts to see if they were reachable from the web
server running on a random port near `7000`.

Files for this level can be found in `/home/flag07`.

Run `getflag` as `flag07` to complete this level.
